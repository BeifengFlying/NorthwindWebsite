# Changed

## 2026-08-06

- 增加首页 `The Path Beneath Our Feet` 模块的中英双语编辑式布局，采用四行英文主标题、描边字、编号正文和错位排版，并完成 320px / 390px 手机适配。
- 移除首页背景路线线条，保留无灰框的留白层次；将动态星空背景与漂移、路线摆动和滚动衍生效果迁移到 AI Lab。
- 完善首页与 AI Lab 的中英文语言切换，保护双语对应文案和品牌化英文内容不被自动误译。

## 2026-08-01 · v1.2.0

- 增加完整中英文双语版本，使用统一的 `zh.json` / `en.json` 语言数据和无刷新切换。
- 统一首页、题解、AI Lab、摄影、工作流、开发旅程和 404 页的响应式导航布局。
- 修复窄屏导航胶囊、返回操作、英文标题和语言切换的溢出与缓存问题。
- 增加首页 Bubble Menu 风格的气泡导航，支持透明背景、逐项弹出动画和移动端适配。
- 调整题解与 AI Lab 的深色视觉方案，移除高疲劳噪点和网格背景，提升低分辨率可读性。
- 保留音乐名等品牌化英文设计元素，中文内容和既有页面结构继续由统一数据驱动。

## 2026-07-31

- 修复首页蓝色 Hero 图形被鼠标跟随与滚动动画同时改写坐标导致的偶发跳动；放大题解筛选工具栏、搜索框及文字，提高深色界面的可读性。
- 将 Crafted Works 返回恢复重构为共享页面状态模块：转场拦截导航前同步保存页面、滚动高度和作品 ID，返回时等待图片、字体及布局稳定，再同步 Lenis、ScrollTrigger、卡片高亮与 BFCache 页面状态。
- 优化全站体验：Crafted Works 返回时恢复离开位置并同步滚动动效、音乐数据迁移至独立 JSON 并增加网易云/QQ/Spotify App 唤起与网页/复制链接兜底、为低分辨率屏幕提升正文和辅助信息可读性。
- 补充发布工作流：每次推送前必须更新 `changed.md`，并在提交前检查状态、差异、空白和完整构建结果。
- 集成开发旅程的 React InfiniteMenu 交互组件，增加六阶段卡片的拖拽旋转、惯性、吸附、键盘导航与响应式展示，并补充 React、React DOM、gl-matrix 和 esbuild 构建依赖。
- 统一首页、摄影、题解、AI Lab、工作流、旅程和 404 页顶部固定导航的磨砂透明度，保持模糊、边框和滚动收缩效果不变。
- 修复首次打开首页时必须进入一次子页面才有音效的问题；音频上下文不再提前创建，而是在声音按钮的按下手势中创建并恢复，静态音频仍会在首屏后台预取。
- 修复 Edge 在滚动首页时背景彩色光晕闪烁的问题，移除与鼠标跟随争抢变换属性的滚动视差，并将固定光晕收敛到独立合成层。
- 修复首次进入时声音按钮需等待页面解析后才响应的问题；声音控制事件现在于首屏渲染前完成绑定，按钮状态随点击即时切换，首次访问默认静音，并保留跨页面设置。
- 恢复项目最初使用的 15 毫秒音乐轮盘刻度声，移除后续生成的下坠式音效。
- 根据交互含义澄清恢复页面转场音效；声音开关移至页面右下角，本身不再播放提示音，并支持即时开关与跨页面保存静音状态。
- 将纸飞机风声改为更短、更轻的高频莎莎声，并将播放增益降至 0.18–0.20。
- 修复 Safari 将触控板滚动视为非音频授权手势而导致纸飞机风声和音乐轮盘声静音的问题，增加导航栏声音开关并在真实点击时预热、恢复 Web Audio。
- 为根路径增加强制重新验证响应头，并为共享音效脚本、首页脚本、转场样式及公开音频增加版本参数，绕过 Cloudflare Worker 和浏览器中遗留的长期缓存与旧 404。
- 页面转场音效改为由动画开始后的独立 620 毫秒定时器触发，并缩短尾音，确保声音不会领先遮罩动画。

## 2026-07-30

- 将音乐轮盘刻度声与纸飞机风声改为 `public/assets/audio/` 下的公开 WAV 资源，并在页面加载时预取，提升 Safari 与 Cloudflare 环境中的播放可靠性。
- 将页面切换音效延迟由 0.14 秒增加至 0.40 秒，使声音在遮罩动画已经明显运动后进入。
- 将页面切换音效延后到几何遮罩开始显现的时间点，使声音与转场动画同步。
- 将音乐轮盘刻度声迁移到用户手势解锁的共享 Web Audio 引擎，避免 Cloudflare 部署后因音频上下文或内嵌资源导致静音。
- 在首页与“关于我”之间的纸飞机滚动过场加入双向风声音效，并限制为每次进入过场区域播放一次。
- 调整 Cloudflare 静态资源缓存策略，防止未使用内容哈希的脚本长期命中旧版 `immutable` 缓存。
- 改进手机端导航、触控尺寸、内容间距、音乐轮盘长文本、滚动显现性能与横向溢出控制。
- 解耦首页动效库与音乐功能，外部 GSAP/Lenis CDN 不可用时仍可选歌、切换封面并播放刻度声。
- 关闭本地预览静态资源缓存，避免调试时误用旧版脚本。

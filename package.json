{
  "name": "northwind-personal-site",
  "version": "1.0.0",
  "private": true,
  "description": "Northwind personal website",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node scripts/serve.mjs --port 4173",
    "generate:sfx": "node scripts/generate-sfx.mjs",
    "preview": "node scripts/serve.mjs --port 4173",
    "check": "npm run build && node scripts/check-project.mjs",
    "optimize:images": "node scripts/optimize-images.cjs"
  },
  "devDependencies": {
    "sharp": "^0.34.3"
  },
  "engines": {
    "node": ">=20"
  }
}
